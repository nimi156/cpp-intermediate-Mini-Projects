/*
In this program, an integer entered by user is stored in variable n, then forloop is executed with an initial condition i=1 and checked whether n is perfectly divisible by i then, 
i will be the factor of n.
In each iteration, the value of i is updated (increased by 1).

*/


#include <iostream>
using namespace std;
int main()
{
int n,i ; // declaring two varibles n and i
cout <<"Enter a Postive Integer:    " ;
cin >> n; // stores value in n
cout << "Factors of  " << n << " are:  " <<endl;

for (i=1; i<n; ++i)
{
    if(n % i==0)
    cout << i <<endl;
}
return 0;
}
